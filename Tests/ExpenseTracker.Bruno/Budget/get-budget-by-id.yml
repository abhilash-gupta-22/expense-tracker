info:
  name: Get Budget By Id
  type: http
  seq: 7

http:
  method: GET
  url: https://localhost:5001/api/budgets/{{budgetId}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Retrieves a single budget by id. Replace {{budgetId}} with a real GUID or set it in the collection variables.
