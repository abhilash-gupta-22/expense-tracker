info:
  name: Delete Expense
  type: http
  seq: 5

http:
  method: DELETE
  url: https://localhost:5001/api/expenses/{{existingId}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Deletes an expense by id. Replace {{existingId}} with a real GUID or set it in the collection variables.
