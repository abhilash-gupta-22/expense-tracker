info:
  name: Get All Expenses
  type: http
  seq: 1

http:
  method: GET
  url: https://localhost:5001/api/expenses

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Retrieves all expenses from the ExpenseTracker API.
