info:
  name: delete-category
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:{{port-number}}/api/categories/{{category-id}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Deletes a category by id.

expect:
  - status: 204

# Verify deletion
verify:
  - method: GET
    url: /categories/{{category-id}}
    expect:
      - status: 404
