info:
  name: get-all-expenses
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{port-number}}/api/expenses

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Retrieves all expenses.

expect:
  - status: 200
  - jsonpath:
      path: $[*].id
      exists: true
